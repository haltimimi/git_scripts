import re

# EXAMPLE:
# >>> find_words(4, "dog, cat, baby, balloon, me")
# ['baby', 'balloon']
def find_words(count, string):
  # regex = r"\b\w{" + re.escape(str(count)) + "}\b" 
  rege
  return [v for v in re.findall(regex, string) ]

